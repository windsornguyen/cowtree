# @generated by Hollywood. Do not edit by hand.

name: CI
on:
  push:
    branches:
      - main
  pull_request: {}
permissions:
  contents: read
jobs:
  workflows:
    name: Generated workflows
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
        with:
          node-version: "24"
      - name: Install workflow dependencies
        run: npm ci --ignore-scripts
        shell: bash
      - name: Check typed workflows and generated files
        run: npm run ci check
        shell: bash
  schema:
    name: Declarative schema
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    env:
      COWTREE_ATLAS: ${{ format('{0}/.tools/atlas', github.workspace) }}
      GOTOOLCHAIN: local
      ATLAS_NO_UPDATE_NOTIFIER: "1"
      ATLAS_NO_UPGRADE_SUGGESTIONS: "1"
    steps:
      - uses: actions/checkout@v6
      - uses: actions/checkout@v6
        name: Check out Atlas Community source
        with:
          repository: ariga/atlas
          ref: 9a6bc601212130aaaefcbc8dd36c710baf9716ff
          path: .tools/atlas-source
          persist-credentials: false
      - uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16
        with:
          go-version: 1.26.5
          cache-dependency-path: .tools/atlas-source/**/go.sum
      - name: Build Atlas Community
        run: go build -mod=readonly -trimpath -o "$HOLLYWOOD_COMMAND_ARG_4" .
        working-directory: .tools/atlas-source/cmd/atlas
        shell: bash
        env:
          HOLLYWOOD_COMMAND_ARG_4: ${{ format('{0}/.tools/atlas', github.workspace) }}
      - uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4
        with:
          enable-cache: true
          cache-dependency-glob: uv.lock
      - name: Install Python test dependencies
        run: uv sync --locked --group test
        shell: bash
      - name: Check generated SQLite schema
        run: uv run python scripts/schema.py check --atlas .tools/atlas
        shell: bash
      - name: Test declarative schema generation
        run: uv run pytest -q tests/test_schema_generation.py
        shell: bash
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4
        with:
          enable-cache: true
          cache-dependency-glob: uv.lock
      - name: Install Python
        run: uv python install 3.10
        shell: bash
      - name: Install dependencies
        run: uv sync --locked --group lint --group bench
        shell: bash
      - name: Ruff lint
        run: uv run ruff check .
        shell: bash
      - name: Ruff format
        run: uv run ruff format --check .
        shell: bash
      - name: Ty typecheck
        run: uv run ty check
        shell: bash
  test:
    name: Test
    runs-on: ${{ matrix.os }}
    env:
      UV_PYTHON: ${{ matrix.python-version }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
        python-version:
          - "3.10"
          - "3.11"
          - "3.12"
          - "3.13"
          - "3.14"
    steps:
      - uses: actions/checkout@v6
      - uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4
        with:
          enable-cache: true
          cache-dependency-glob: uv.lock
      - name: Install Python
        run: uv python install "$HOLLYWOOD_COMMAND_ARG_2"
        shell: bash
        env:
          HOLLYWOOD_COMMAND_ARG_2: ${{ matrix.python-version }}
      - name: Install dependencies
        run: uv sync --locked --group test
        shell: bash
      - name: Verify selected Python
        run: uv run python -c 'import os, sys; actual = f'"'"'{sys.version_info.major}.{sys.version_info.minor}'"'"'; print(sys.version); assert actual == os.environ['"'"'UV_PYTHON'"'"'], actual'
        shell: bash
      - name: Run tests
        run: uv run pytest
        shell: bash
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4
        with:
          enable-cache: true
          cache-dependency-glob: uv.lock
      - name: Install Python
        run: uv python install 3.10
        shell: bash
      - name: Build package
        run: uv build
        shell: bash
  filesystems:
    name: Native filesystem matrix
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
      - uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4
      - name: Native filesystem matrix
        uses: ./.github/actions/ci/native-filesystems
  windows:
    name: Windows ReFS native cloning
    runs-on: windows-2025
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
      - uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4
      - name: Install Python
        run: uv python install 3.10
        shell: bash
      - name: Install test dependencies
        run: uv sync --locked --group test
        shell: bash
      - name: Check ReFS isolation and NTFS refusal
        run: pwsh -NoProfile -File scripts/test_windows.ps1
        shell: bash
  specification:
    name: Workspace protocol model
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
      - uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4
      - uses: actions/setup-java@de7274f081f381c8f8158605e0321c36c376e2e6
        with:
          distribution: temurin
          java-version: "17"
      - name: Check safety, witnesses, and fencing mutation
        run: uv run --locked --no-default-groups python scripts/check_specs.py --cache "$HOLLYWOOD_COMMAND_ARG_6"
        shell: bash
        env:
          HOLLYWOOD_COMMAND_ARG_6: ${{ format('{0}/cowtree-tla', runner.temp) }}
      - name: Save checker evidence
        if: ${{ always() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: workspace-protocol-evidence
          if-no-files-found: error
          path: |-
            ${{ runner.temp }}/cowtree-tla/java-version.txt
            ${{ runner.temp }}/cowtree-tla/*/tlc.log
            ${{ runner.temp }}/cowtree-tla/*/counterexample.json
            ${{ runner.temp }}/cowtree-tla/*/*.tla
            ${{ runner.temp }}/cowtree-tla/*/*.cfg
