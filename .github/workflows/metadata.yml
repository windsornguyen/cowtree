# @generated by Hollywood. Do not edit by hand.

name: SQLite metadata
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
permissions:
  contents: read
jobs:
  metadata:
    name: ${{ format('SQLite ({0})', matrix.os) }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-24.04
          - macos-latest
    steps:
      - uses: actions/checkout@v6
      - name: Install Rust
        run: rustup toolchain install 1.97.1 --profile minimal --component rustfmt --component clippy
        shell: bash
      - name: Format
        run: cargo +1.97.1 fmt --all --check
        shell: bash
      - name: Lint
        run: cargo +1.97.1 clippy --locked --workspace --all-targets --all-features -- -D warnings
        shell: bash
      - name: Update Linux package index
        run: sudo apt-get update
        if: ${{ runner.os == 'Linux' }}
        shell: bash
      - name: Install Linux reflink test filesystem
        run: sudo apt-get install -y btrfs-progs
        if: ${{ runner.os == 'Linux' }}
        shell: bash
      - name: Test crashes and concurrent publication on native reflinks
        run: bash scripts/metadata_test.sh
        shell: bash
      - uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4
        with:
          enable-cache: true
          cache-dependency-glob: uv.lock
        if: ${{ runner.os == 'macOS' }}
      - name: Test managed Python workspaces on APFS
        run: uv run --locked --no-default-groups --group test pytest tests/test_workspace.py -q
        if: ${{ runner.os == 'macOS' }}
        env:
          COWTREE_METADATA_BINARY: ${{ format('{0}/target/debug/cowtree-metadata', github.workspace) }}
          COWTREE_EXPECT_SUPPORTED: "1"
        shell: bash
      - name: Run production example with test hooks disabled
        run: cargo +1.97.1 run --locked -p cowtree-metadata --example publish
        env:
          COWTREE_CRASH_AT: after-sql-commit
          COWTREE_PAUSE_AT: after-upload-pin
        shell: bash
      - name: Check documentation
        run: cargo +1.97.1 doc --locked --workspace --no-deps
        env:
          RUSTDOCFLAGS: -D warnings
        shell: bash
  minimum-rust:
    name: SQLite minimum Rust
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
      - name: Install minimum Rust
        run: rustup toolchain install 1.85.0 --profile minimal
        shell: bash
      - name: Check minimum supported compiler
        run: cargo +1.85.0 check --locked --workspace --all-targets --all-features
        shell: bash
