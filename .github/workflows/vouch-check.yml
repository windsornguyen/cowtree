# @generated by Hollywood. Do not edit by hand.

name: "Vouch: Check PR"
on:
  pull_request_target:
    types:
      - opened
      - reopened
      - synchronize
  workflow_dispatch:
    inputs:
      pr-number:
        description: Open PR whose same-repository head branch is selected for this run
        required: true
        type: number
permissions:
  pull-requests: write
  contents: read
jobs:
  check:
    name: Vouch eligibility
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
        name: Read the trusted contribution policy and actions
        with:
          ref: ${{ github.event.repository.default_branch }}
          persist-credentials: false
      - id: request
        env:
          GH_TOKEN: ${{ github.token }}
        name: Resolve the requested PR
        uses: ./.github/actions/ci/resolve-vouch-pr
        with:
          event: ${{ github.event_name }}
          dispatch-pr: ${{ inputs.pr-number }}
          event-pr: ${{ github.event.pull_request.number }}
          repository: ${{ github.repository }}
          sha: ${{ github.sha }}
      - uses: mitchellh/vouch/action/check-pr@d66fa29a64600490892131ad87597c30c91fcac4
        id: vouch
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          pr-number: ${{ steps.request.outputs.number }}
          require-vouch: true
          auto-close: true
          vouched-file: VOUCHED.td
          template-file: .github/VOUCH_RESPONSE.md
      - name: Require contributor access
        uses: ./.github/actions/ci/require-vouch
        with:
          status: ${{ steps.vouch.outputs.status }}
