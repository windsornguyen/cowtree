\* Reachable-state check of the core. Small and fast.
SPECIFICATION Spec
CONSTANTS
  Leaves = {l1, l2}
  Keys = {k1, k2}
  Values = {v0, v1}
  MaxToken = 3
SYMMETRY Symm
CONSTRAINT TokenBound
INVARIANTS Inv
PROPERTIES NoLostUpdate FencedOut MergesOnlyOnLateGrant
CHECK_DEADLOCK FALSE
