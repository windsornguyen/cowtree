\* Enumerate InvBounded (empty merge history) and check Inv on bounded successors.
INIT InvBounded
NEXT Next
CONSTANTS
  Leaves = {l1, l2}
  Keys = {k1}
  Values = {v0, v1}
  MaxToken = 2
CONSTRAINT TokenBound
INVARIANTS Inv
CHECK_DEADLOCK FALSE
