\* Larger reachable instance; not qualified by the default runner. See specs/README.md.
SPECIFICATION Spec
CONSTANTS
  Leaves = {l1, l2}
  Paths = {p1, p2}
  Values = {v0, v1}
  MaxNodes = 3
  None = None
SYMMETRY Symm
INVARIANTS Inv
PROPERTIES ContentImmutable TipFastForwardOnly NoLostUpdate MergesOnlyOnStale
CHECK_DEADLOCK FALSE
