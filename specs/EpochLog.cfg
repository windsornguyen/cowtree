\* Induction step to time n: several commits. Large; run on a real machine.
SPECIFICATION Spec
CONSTANTS
  Leaves = {l1, l2}
  Paths = {p1, p2}
  Values = {v0, v1}
  MaxEpochs = 3
  MaxToken = 3
  None = None
SYMMETRY Symm
CONSTRAINT TokenBound
INVARIANTS Invariants
PROPERTIES LogAppendOnly NoLostUpdate FencedOut MergesOnlyOnStale
CHECK_DEADLOCK FALSE
